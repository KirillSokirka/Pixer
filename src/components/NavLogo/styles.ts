import styled from "styled-components";
import IOPenable from "../interfaces";

const SNavLogo = styled.div<IOPenable>`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export { SNavLogo };
